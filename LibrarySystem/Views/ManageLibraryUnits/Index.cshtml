@model List<LibrarySystem.Models.Domain.LibraryUnit>

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<a asp-controller="ManageLibraryUnits" asp-action="Upsert" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Create</a>


<table class="table">
    <thead>
        <tr>
            <th>
                Title
            </th>
            <th>
                Current Condition
            </th>
             <th>
                Medium
            </th>
            <th>
                Place
            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Title.Name
                </td>
                <td>
                    @item.CurrentCondition
                </td>
                <td>
                    @item.Medium
                </td>
                <td>
                    @item.Place
                </td>
                <td>
                    <a asp-controller="ManageLibraryUnits" asp-action="Upsert" class="btn btn-secondary" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i> Edit</a>
                    <a asp-controller="ManageLibraryUnits" asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Id"><i class="bi bi-trash"></i> Delete</a>
                </td>
            </tr>
        }
    </tbody>

</table>